name: data_analysis_user
description: "Data analysis user message template"
variables: [csv_path, columns, algorithms_desc, functions_desc]
template: |
  CSV File: ${csv_path}
  Columns: ${columns}

  Available Algorithms:
  ${algorithms_desc}

  ${functions_desc}

  Please analyze the above column names and determine the most suitable algorithm and reading method.
